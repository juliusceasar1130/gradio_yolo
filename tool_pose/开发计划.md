# 关键点角度计算功能实现方案

<!-- 
开发计划文档
创建时间：2025年1月28日
最后更新：2025年11月2日
状态：已完成
-->

## 🎯 目标

在 `pose_predict.py` 中集成关键点角度计算功能，能够：

1. ✅ 从配置文件读取角度定义
2. ✅ 根据检测到的对象类别和关键点计算角度
3. ✅ 在可视化图像上标注角度值（原图坐标）
4. ✅ 输出角度结果到控制台和文件

## 📋 实现步骤

### 1. ✅ 创建角度配置文件 `angle_config.yaml`

- **位置**: `tool_pose/tools/angle_config.yaml`
- **状态**: ✅ 已完成
- **格式**: 
  ```yaml
  tool1:
    angle1: [t1_1, t1_2, t1_3]
    angle2: [t1_2, t1_3, t1_4]
    angle3: [t1_3, t1_4, t1_5]
    angle4: [t1_4, t1_5, t1_6]
    angle5: [t1_5, t1_6, t1_7]
  tool2:
    angle1: [t2_1, t2_2, t2_3]
    angle2: [t2_2, t2_3, t2_4]
    angle3: [t2_3, t2_4, t2_5]
    angle4: [t2_4, t2_5, t2_6]
    angle5: [t2_5, t2_6, t2_7]
  ```
- **说明**: 每个角度由三个关键点名称定义，中间点为顶点

### 2. ✅ 创建角度计算工具模块 `angle_calculator.py`

- **位置**: `tool_pose/tools/angle_calculator.py`
- **状态**: ✅ 已完成
- **核心功能**:
  - ✅ `load_angle_config(config_path)`: 加载角度配置文件
  - ✅ `load_tool_pose_config(config_path)`: 加载工具姿态配置文件
  - ✅ `get_keypoint_index_by_name(kpt_name, kpt_names_list)`: 通过名称字符串匹配获取关键点索引
  - ✅ `calculate_angle(p1, p2, p3)`: 计算三点角度（p2为顶点），返回角度（度）
  - ✅ `calculate_angles_for_object(...)`: 为单个对象计算所有角度
  - ✅ `draw_angle_arc(...)`: 绘制角度弧线
  - ✅ `annotate_angles_on_image(...)`: 在图像上标注角度值

### 3. ✅ 修改 `pose_predict.py`

- **状态**: ✅ 已完成
- **集成内容**:
  - ✅ 加载 `angle_config.yaml` 和 `tool_pose.yaml` 配置文件
  - ✅ 对每个检测到的对象计算角度
  - ✅ 在可视化图像上绘制角度标注（原图坐标）
  - ✅ 在控制台打印角度结果
  - ✅ 保存角度结果到 JSON 文件
  - ✅ 实现 JSON 序列化处理（处理 NumPy 类型）

### 4. ✅ 数据结构设计

- **状态**: ✅ 已实现
- **角度结果格式**:
  ```python
  {
    'object_id': 0,
    'class_id': 0,
    'class_name': 'tool1',
    'angles': {
      'angle1': {
        'value': 116.29,
        'keypoints': ['t1_1', 't1_2', 't1_3'],
        'keypoint_indices': [0, 1, 2],
        'keypoint_positions': {
          't1_1': [886.11, 1136.44],
          't1_2': [1031.27, 894.48],
          't1_3': [1419.13, 926.17]
        },
        'valid': True
      },
      'angle2': {
        'value': None,
        'keypoints': [...],
        'valid': False,
        'reason': '关键点不可见: t1_4'
      }
    }
  }
  ```

### 5. ✅ 图像标注实现

- **状态**: ✅ 已完成
- **坐标系统**: ✅ 使用原图坐标（`result.keypoints.xy` 返回的像素坐标，对应原图尺寸）
- **标注位置**: ✅ 在原图可视化结果上绘制角度信息
- **标注内容**:
  - ✅ 在角度顶点（中间关键点）附近显示角度值（度）
  - ✅ 绘制角度弧线（从边1到边2，顶点在中心）
  - ✅ 使用绿色表示有效角度
  - ✅ 显示角度名称（如 "angle1: 116.3°"）
- **标注样式**:
  - ✅ 角度值文本：字体大小适中，背景色半透明便于识别
  - ✅ 角度弧线：绿色，半径25像素，线条粗细2像素
  - ✅ 文本位置：角度顶点附近偏移（+15, -15像素）

### 6. ✅ 文件输出

- **状态**: ✅ 已完成
- **JSON格式**: ✅ 保存完整的角度计算结果
- **文件命名**: ✅ `angle_results_YYYYMMDD_HHMMSS.json`
- **文件位置**: ✅ `tool_pose/outputs/`

## 🔧 关键技术点

### ✅ 三点角度计算公式

使用向量夹角公式：

```python
# p2为顶点，计算向量p2->p1和p2->p3的夹角
vec1 = p1 - p2
vec2 = p3 - p2
norm1 = np.linalg.norm(vec1)
norm2 = np.linalg.norm(vec2)
cos_angle = np.dot(vec1, vec2) / (norm1 * norm2)
cos_angle = np.clip(cos_angle, -1.0, 1.0)  # 防止数值误差
angle_rad = np.arccos(cos_angle)
angle_deg = np.degrees(angle_rad)
```

### ✅ 关键点名称映射

- ✅ 从 `tool_pose.yaml` 读取 `kpt_names` 配置
- ✅ 根据对象类别ID获取对应的关键点名称列表
- ✅ 通过字符串匹配找到角度配置中关键点名称对应的索引

### ✅ 可见性检查

- ✅ 检查三个关键点的可见性标志（visibility > 0.5）
- ✅ 如果任一关键点不可见，返回 `None` 并记录原因
- ✅ 在控制台输出详细的错误信息

### ✅ JSON序列化处理

- ✅ 实现 `convert_to_json_serializable()` 函数
- ✅ 处理 NumPy 类型转换（integer, floating, bool_, ndarray）
- ✅ 递归处理字典和列表

## 📁 文件清单

### ✅ 新增文件

1. ✅ `tool_pose/tools/angle_config.yaml` - 角度配置文件
2. ✅ `tool_pose/tools/angle_calculator.py` - 角度计算工具模块（433行）

### ✅ 修改文件

1. ✅ `tool_pose/pose_predict.py` - 集成角度计算功能（374行）

### ✅ 输出文件（运行时生成）

1. ✅ `tool_pose/outputs/angle_results_*.json` - 角度结果JSON文件

## ⚠️ 遇到的问题与解决方案

### 问题1: JSON序列化错误

**错误**: `TypeError: Object of type bool is not JSON serializable`

**原因**: NumPy的`numpy.bool_`类型无法直接JSON序列化

**解决方案**:
- ✅ 创建`convert_to_json_serializable()`函数递归转换
- ✅ 显式转换布尔值: `bool(vis > 0.5)`
- ✅ 显式转换索引类型

### 问题2: OpenCV椭圆绘制角度问题

**问题**: 角度弧线绘制方向不正确

**原因**: OpenCV的ellipse使用顺时针角度，而arctan2返回逆时针角度

**解决方案**:
- ✅ 角度取负并转换为[0, 360)范围
- ✅ 添加异常处理，静默跳过绘制失败的情况

### 问题3: 关键点不可见处理

**需求**: 当关键点不可见时，应跳过角度计算并给出提示

**实现**: 
- ✅ 检查三个关键点的visibility值
- ✅ 如果任一关键点visibility < 0.5，返回`valid: False`并记录原因
- ✅ 在控制台输出详细的错误信息

## 📊 测试结果

### 测试环境
- ✅ 运行环境: gradioflask (conda)
- ✅ Python版本: 3.9/3.10
- ✅ 测试图像: tool1类别工具图像
- ✅ 关键点数量: 7个

### 测试结果
- ✅ 成功计算5/5个角度
- ✅ 角度值范围合理（约79°-122°）
- ✅ 图像标注清晰可见
- ✅ JSON文件格式正确
- ✅ 无运行时错误

### 示例输出

```
📏 对象 1 (tool1) 的角度:
   angle1: 116.29° ✓
   angle2: 121.72° ✓
   angle3: 113.30° ✓
   angle4: 79.69° ✓
   angle5: 101.05° ✓
   ✅ 成功计算 5/5 个角度
```

## 📝 完成状态

### 已完成项目

- [x] 角度配置文件创建
- [x] 角度计算工具模块实现（433行代码）
- [x] 主预测脚本集成（374行代码）
- [x] 图像角度标注实现（原图坐标）
- [x] JSON结果输出
- [x] 错误处理和异常情况处理
- [x] NumPy类型JSON序列化问题修复
- [x] OpenCV角度绘制问题修复
- [x] 完整测试和验证

### 代码统计

- **新增代码行数**: 约800行
- **配置文件**: 2个（angle_config.yaml, tool_pose.yaml）
- **核心模块**: 1个（angle_calculator.py）
- **测试用例**: 已通过实际图像测试**

## 🔄 未来改进方向（可选）

1. **角度范围验证**: 添加角度合理性检查（如0-180度范围外的情况）
2. **多角度重叠处理**: 当多个角度标注重叠时，优化布局
3. **角度趋势分析**: 支持批量图像的角度统计分析
4. **配置文件验证**: 添加角度配置的完整性验证
5. **性能优化**: 批量处理时的性能优化
6. **CSV格式输出**: 可选，便于数据分析

## 📚 相关文档

- `tool_pose/关键点角度计算功能实现总结.md` - 详细实现总结文档
- `tool_pose/tools/角度计算需求.md` - 原始需求文档
- `tool_pose/tools/tool_pose.yaml` - 工具姿态配置
- `tool_pose/预测结果数据结构解读.md` - YOLO Pose数据结构说明
- `tool_pose/outputs/angle_results_*.json` - 示例输出文件

---

**计划制定时间**: 2025年1月28日  
**实现完成时间**: 2025年1月31日  
**最后更新**: 2025年1月31日  
**状态**: ✅ **全部完成**

